@using MangaWorkflow.Application.DTOs.Dashboard
@using MangaWorkflow.Application.Interfaces.Services
@inject IDashboardService DashboardService

<div class="row row-cols-1 row-cols-md-2 g-4 mb-4">
    <div class="col">
        <div class="card bg-warning text-dark h-100">
            <div class="card-body">
                <h5 class="card-title">Pending Votes</h5>
                <h2 class="mb-0">@Model?.PendingVotesCount</h2>
            </div>
        </div>
    </div>
    <div class="col">
        <div class="card bg-success text-white h-100">
            <div class="card-body">
                <h5 class="card-title">Series Reviewed</h5>
                <h2 class="mb-0">@Model?.TotalSeriesReviewed</h2>
            </div>
        </div>
    </div>
</div>

@code {
    [Parameter]
    public Guid UserId { get; set; }

    private BoardDashboardDto? Model;

    protected override async Task OnInitializedAsync()
    {
        Model = await DashboardService.GetBoardDashboardAsync(UserId);
    }
}
